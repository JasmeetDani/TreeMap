using UnityEngine;

public static class Constants
{
    public const float defaultHeaderPercentage = 0.1f;

    public const int minPadding = 1;

    public static readonly Color primitiveBorder = new Color(175 / 255.0f, 175 / 255.0f, 175 / 255.0f, 125 / 255.0f);

    public static readonly Color headerStartColor = new Color(225 / 255.0f, 225 / 255.0f, 225 / 255.0f);

    public const int headerColorSubtractor = 20;
}